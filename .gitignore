# Data (regenerable, large, or synthetic)
data/training_raw.parquet
data/predictions/
data/_checkpoints/
# NOTE: data/predictions_sample/ is intentionally NOT ignored -- it's the
# small versioned sample the deployed dashboard reads (see
# scoring/export_sample.py). Do not add a broader "data/" rule above that
# would accidentally catch it.

# Trained model artifact (binary, regenerable via training/train_model.py)
training/model.joblib

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Conda / venv
.conda/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
